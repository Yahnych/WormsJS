export class Weapon {

    name: string
    damage: number
    image: string

    constructor(name: string, damage: number, image: string) {
        this.name = name
        this.damage = damage
        this.image = image
    }
}